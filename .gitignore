# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — keep secrets out of git; .env.production holds public Vite keys only
.env
.env.local
# .env.production is committed intentionally (VITE_* publishable keys only)

# Migration secrets / dumps
serviceAccount.json
scripts/migrate/data/*.json
!scripts/migrate/data/.gitkeep
